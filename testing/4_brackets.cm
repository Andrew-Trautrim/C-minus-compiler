/* invalid brackets */
int func1 void) {
    int a[1 ;
    int b 1];

    a = -(5 + 6);
}

/* valid brackets */
int func2(void) {
    int a;
    int b;

    b = ((((((((1))))))));
    a = -(1 / (5 * (b + -1)));
    while ((a < 0 || a > 10) && ~(false || a == 25)) {
        if (a == 42) {
            a = do_something(52 / a * 7);
        } 
        else {
            a = a - 1;
        }
    }

    return 0;
}

int do_something(int a) {
    /* does soething */
    return a;
}
