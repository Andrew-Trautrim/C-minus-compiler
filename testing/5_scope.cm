

int func(int a, bool b) {
    int i;
    int j;

    /* variable declared twice */
    int i;

    /* recursive call without function prototype */
    i = func(a - 1, b);
    
    if (b) {
        int k;
        k = 5;
    }

    /* out of scope reference */
    j = k;

    return j;
}