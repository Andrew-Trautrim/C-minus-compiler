/* prototype */
int func1(int a, bool b);
int func2(int a, bool b);

/* definitions */
int func1(int a, bool b) {
    int i;
    int j;

    i = func2();
    if (2 / (a + 1) * 5 <= 72 || ~(7 > i) && ~b) {
        return i + j / (i * 4);
    }

    return 100;
}

int func2(int a, bool b) {
    if (~b) {
        return a;
    }
    else {
        return 1 - a;
    }
}